return {
  cmd = { "fennel-ls" },
  filetypes = { "fennel" },
  root_markers = { "flsproject.fnl", ".git" },
  single_file_support = true,
  settings = {},
  capabilities = {
    offsetEncoding = { "utf-8", "utf-16" }
  }
}
